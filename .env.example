# Leave empty in local dev — Vite proxies /api → http://localhost:4000
# In production (Vercel), set to your backend origin (no trailing slash), e.g.:
#   VITE_API_URL=https://your-backend.example.com
# Backend must allow this frontend origin via CORS_ORIGIN.
VITE_API_URL=
